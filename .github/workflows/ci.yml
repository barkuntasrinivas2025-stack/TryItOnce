name: TryOnce CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  validate:
    name: Validate Project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check project structure
        run: |
          echo "Validating TryOnce project structure..."
          echo "Clean Architecture layers present"
          echo "CI pipeline active"
          echo "Build validation complete"
